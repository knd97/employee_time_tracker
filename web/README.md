```bash
#start docker daemon manually
$ dockerd
#inside web directory
$ sudo docker-compose build
$ sudo docker-compose up
#web application available at local ip address
#check local ip address
$ ip a
#or
$ ifconfig
```
